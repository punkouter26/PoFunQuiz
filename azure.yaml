# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoFunQuiz
metadata:
  template: PoFunQuiz@1.0.0

# Azure Developer CLI will deploy from the Aspire AppHost
infra:
  provider: bicep
  path: infra

services:
  pofunquiz-web:
    project: src/PoFunQuiz.AppHost/PoFunQuiz.AppHost.csproj
    host: containerapp
    language: dotnet

