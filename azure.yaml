# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoFunQuiz
metadata:
  template: PoFunQuiz@1.0.0

services:
  PoFunQuiz:
    project: src/PoFunQuiz.Api
    host: appservice
    language: dotnet

