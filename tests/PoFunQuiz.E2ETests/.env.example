# E2E Test Configuration

# Test environment: 'local' or 'azure'
TEST_ENV=local

# Azure App URL (used when TEST_ENV=azure)
AZURE_APP_URL=https://pofunquiz.azurewebsites.net

# Local development URL (used when TEST_ENV=local)
BASE_URL=http://localhost:5000
